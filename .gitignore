# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Vercel
.vercel

# Node (only relevant if you ever add tooling)
node_modules/
npm-debug.log*

# Opt-in obfuscated build output (scripts/build-obfuscated.js) —
# generated locally/in CI, never committed.
dist/

# Local env files (see .env.example — not required for this project,
# only relevant if you use the optional scripts/inject-env.js workflow)
.env
.env.local
